# Hytale Assets

This repository tracks changes in Hytale server assets. The default `main` branch tracks `release` patchline. In order to view changes in `pre-release` please check `pre-release` branch.
